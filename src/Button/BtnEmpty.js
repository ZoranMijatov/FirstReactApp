import styled from 'styled-components'

const BtnEmpty  = styled.button`
background: transparent;
border-radius: 10px;
background: white;
border: 2px solid #39A2DB;
color: #39A2DB;
font-weight: bold;
font-size: 16px;
margin: 0 1em;
padding: 0.25em 1em;
padding: 16px 34px;
box-shadow: 3px 4px 17px -12px rgba(0,0,0,0.75);
    &:hover{
        cursor: pointer;
    }
`
export default BtnEmpty;